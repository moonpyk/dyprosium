To compile Dyprosium basically with default spec :
--------------------------------------------------

cd src
qmake

make
-or, if you're using VS utils :
nmake

To edit Dyprosium as a Visual Studio project :
--------------------------------------------------

cd src
qmake -t vcapp

Then open generated "Dyprosium.vcproj"

To edit Dyprosium as a XCode project on Mac OS X :
--------------------------------------------------

cd src
qmake -spec macx-xcode

Then open generated "Dyprosium.xcodeproj"

--------------------------------------------------
--------------------------------------------------

Dyprosium executable (or bundle on Mac OSX) is
compiled under this "bin" directory at project's root.



